---
- name: a playbook to setup my local machine with my basic customizations
  hosts: raspberry
  gather_facts: yes
  tasks:
    - set_fact:
        password="{{ lookup('pipe', 'date +%Y%m%d%H%M%S') }}"
  #roles:
  #  - wifi
  #  - packages
  #  - motioneye
  #  - kuman
   #- retropie
   #- kodi
   #- users
   #- bluetooth


