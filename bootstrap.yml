---
- name: a playbook to setup my local machine with my basic customizations
  hosts: localhost
  connection: local
  vars_files:
    - vars/all.yml
    - vars/user.yml
  roles:
    - tmux
    #- vim
    #- bash
    #- pass
    #- dnf
    #- git
    #- ssh
    #- ansible
    #- variety

